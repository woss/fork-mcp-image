{
  "name": "mcp-image",
  "mcpName": "io.github.shinpr/mcp-image",
  "version": "0.13.0",
  "description": "MCP server for AI image generation",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-image": "dist/index.js"
  },
  "keywords": [
    "mcp",
    "mcp-server",
    "model-context-protocol",
    "image-generation",
    "generative-ai",
    "image-editing",
    "prompt-enhancement",
    "gemini",
    "google-ai",
    "openai",
    "gpt-image-2",
    "nano-banana",
    "claude-code",
    "cursor",
    "codex",
    "typescript",
    "agent-skills",
    "skills"
  ],
  "author": "Shinsuke Kagawa",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shinpr/mcp-image.git"
  },
  "files": [
    "dist/**/*",
    "!dist/**/*.test.js",
    "!dist/**/*.test.d.ts",
    "!dist/__tests__",
    "!dist/tests",
    "bin",
    "skills"
  ],
  "scripts": {
    "prepare": "husky",
    "build": "tsc",
    "link": "npm run build && npm link",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:coverage:summary": "node scripts/show-coverage.js",
    "test:coverage:clean": "rm -rf coverage .vitest-cache",
    "test:coverage:fresh": "npm run test:coverage:clean && npm run test:coverage",
    "test:watch": "vitest",
    "format": "biome format --write src",
    "format:check": "biome format src",
    "lint": "biome lint src",
    "lint:fix": "biome lint --write src",
    "check": "biome check src",
    "check:fix": "biome check --write src",
    "check:deps": "dpdm --no-tree --no-warning --circular --exit-code circular:1 --exclude \"(node_modules|__tests__|\\.test\\.ts$|\\.spec\\.ts$)\" --tsconfig ./tsconfig.json \"src/**/*.ts\"",
    "check:unused": "knip",
    "check:all": "npm run check && npm run lint && npm run format:check && npm run check:deps && npm run check:unused && npm run build && npm run test",
    "cleanup:processes": "bash ./scripts/cleanup-test-processes.sh",
    "test:safe": "npm test && npm run cleanup:processes"
  },
  "dependencies": {
    "@google/genai": "^2.15.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "openai": "^7.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.10",
    "@vitest/ui": "^4.1.10",
    "dpdm": "^4.3.0",
    "husky": "^9.1.7",
    "knip": "^6.31.0",
    "lint-staged": "^17.3.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  },
  "lint-staged": {
    "src/**/*.{ts,tsx}": [
      "biome check --write --no-errors-on-unmatched",
      "biome format --write --no-errors-on-unmatched"
    ]
  }
}
